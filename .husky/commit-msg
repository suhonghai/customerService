#!/usr/bin/env sh
# commit-msg hook:commitlint + check-spec-link 双向校验
# 2026-07-31 §B 落地:commit 含 [change-id] 必能找到 spec
set -e

# 1) commitlint(常规 conventional commits 校验)
pnpm commitlint --edit "$1"

# 2) check-spec-link(我们的 spec ↔ commit 双向校验)
pnpm check-spec-link "$1"
